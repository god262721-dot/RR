# AI_CONTEXT：新AI必读的项目总览

## 1. 你的角色

你正在协助一个计算机视觉研究课题，而不是开发正式软件产品。讨论和建议
必须遵守以下原则：

1. 区分实验事实、综合解释和待验证假设。
2. 不因为用户倾向于双向纠偏，就默认双向纠偏一定成立。
3. 只提出能被低成本实验证伪的设计。
4. 计算资源和时间有限，优先小规模、单变量、带停止条件的实验。
5. 负面结果必须保留，不能只汇报支持预设路线的结果。
6. 不把重复使用的开发测试集称为最终独立测试集。

## 2. 研究目标

任务是偏振引导的RGB图像反射去除。长期候选方向包含：

- 偏振测量置信度；
- 可解释的Polar–RGB融合；
- Polar→RGB与RGB→Polar双向纠偏。

当前暂时仍希望保留“双向纠偏”作为研究假设，但证据尚不支持把它写成已经
成立的主线。当前真正要回答的是：

> 更准确的偏振状态是否能通过合适的任务接口，提高RGB去反射效果？

若答案是否定的，应停止RGB→Polar反馈路线，转向单向Polar→RGB的显式融合
与任务相关注入；若答案为正，才继续设计RGB指导的显式Stokes纠正。

## 3. 数据与偏振表示

数据来自PolaRGB。现有本地协议使用带反射RGB、四角偏振图和无反射RGB GT。
对应GT目录中的四角偏振图目前称为“候选干净偏振GT”，不要超出已核验语义
将其称为数据集官方定义的唯一偏振GT。

四角输入为：

\[
I_0,\ I_{45},\ I_{90},\ I_{135}.
\]

使用：

\[
S_0=\frac{I_0+I_{45}+I_{90}+I_{135}}{2},\quad
S_1=I_0-I_{90},\quad
S_2=I_{45}-I_{135}.
\]

网络使用的稳定归一化表示为：

\[
P_0=S_0/2,\quad
q=\operatorname{clamp}\left(\frac{S_1}{S_0+\epsilon},-1,1\right),\quad
u=\operatorname{clamp}\left(\frac{S_2}{S_0+\epsilon},-1,1\right).
\]

这里`P0=S0/2`是正确的：当四角图已归一化到`[0,1]`时，`S0`范围可到
`[0,2]`，再次除以2将其映射回`[0,1]`。

## 4. 已完成的实验链

### E0：RGB-only

- 输入只有带反射RGB。
- 作用是验证Dataset、训练、评估和RGB恢复主干。
- 训练有效，但提升较弱，不应包装成强基线成绩。

### E1：单向Polar→RGB

- RGB和偏振分别编码。
- 在瓶颈处把偏振特征投影后注入RGB主干。
- correct/zero/shuffled使用同一checkpoint推理。
- correct明显优于zero和shuffled，证明模型不是忽略偏振输入。
- E1-large是当前最强开发基线。

### E2：单轮RGB→Polar→RGB

- 在E1基础上增加约3.7万参数的R2P头。
- R2P从RGB瓶颈预测FiLM式`gamma/beta`，调制偏振潜特征，再由原P2R接口
  反馈给RGB。
- 大规模实验中E2相对E1下降约`0.908 dB`。
- 同checkpoint的normal仅略优于off，说明R2P被调用，但没有形成足够收益。

### TP09：冻结E1，只训练R2P头

- 两个seed，各固定2000步。
- 一个seed的best停在step 0，另一个也没有稳定正增益。
- 数值价值门和偏振特异性门均失败。
- 结论只适用于当前FiLM式R2P头。

### TP10：加入候选偏振GT监督

- 冻结E1主干，只训练R2P与偏振decoder。
- 同时使用角度重建、Stokes和四角成对和闭合监督。
- 偏振恢复任务确实学到了东西，但R2P-on相对off的RGB PSNR在两个seed上
  分别约下降`0.047 dB`和`0.048 dB`。
- 关键结论：缺少偏振GT监督不是充分解释；“偏振预测更准”不会自动变成
  “RGB去反射更好”。

## 5. 当前假设

现有R2P失败可能来自以下结构性冲突，但它们仍是解释，不是已证实因果：

1. R2P指导源来自带反射RGB特征，可能把反射结构重新写入偏振表示。
2. 同一个纠正后偏振潜变量同时承担偏振重建和下游P2R输入，两个目标可能
   要求不同表征。
3. 原P2R接口是在实测偏振分布上训练的，纠正后的偏振可能产生接口分布漂移。
4. 无条件反馈没有回答“更准确偏振是否对当前像素的去反射任务有用”。

用户暂时希望先不引入任务效用门控，仍优先验证双向纠偏本身是否有价值。

## 6. 当前下一步：state-only polar oracle

该实验不预测`q/u`，而是先做价值上限检验：

1. 冻结E1，得到第一阶段结果`T1`。
2. 为所有组使用同结构、同参数量、同初始化的第二阶段任务适配器。
3. 比较：
   - `rgb_only`：新增偏振通道全零；
   - `pobs`：观测`P0/q/u`；
   - `state_gt`：观测`P0`加候选GT `q/u`；
   - `combined`：同时给观测状态、GT状态和差值；
   - `full_gt`：候选GT `P0/q/u`，只作描述性上界。
4. 主判据只看`state_gt`或`combined`相对`pobs`，不能用`full_gt`证明
   `q/u`有效，因为`full_gt`还包含干净强度。
5. 先单seed低成本筛选；通过硬门后才运行第二seed；最后才允许一次冻结
   验证确认。

判定含义：

- PASS：更准确`q/u`在当前任务适配器下存在可观测价值上限，可以继续研究
  RGB指导的显式Stokes纠正。
- STOP：更准确`q/u`也不能改善RGB，应停止R2P反馈主线。
- INCONCLUSIVE：不同seed或不同候选结果不一致，不追加大量调参，应重新
  检查接口和假设。

PASS也不等于已经证明RGB能够恢复干净偏振，更不等于双向纠偏已经成功。

## 7. 数学与实现审计结论

一个旧的“显式Stokes双向纠偏V1”方案已作废。它先把`q/u`硬投影到单位
圆盘，再计算：

\[
\max(0,\hat q^2+\hat u^2-1),
\]

因此该损失恒为零。不要复用这个顺序。

其他已知风险：

- 早期E0/E1/E2的多worker Dataset RNG可能产生相关裁剪；
- 早期运行不是严格确定性；
- TP10在clamp后的四角输出上计算损失，存在饱和梯度风险；
- TP09/TP10脚本中LPIPS胜场计数方向曾写反，但正式门槛没有依赖该字段；
- 开发测试场景已经反复参与路线决策。

这些问题没有推翻当前主要结论，但限制了结论强度。

## 8. 不应建议的事情

在没有新证据前，不要直接建议：

- 增加多轮循环；
- 扩大当前FiLM式R2P头；
- 只调`gamma/beta`幅度或损失权重；
- 直接加入复杂置信度模块；
- 用更低偏振MAE宣称去反射机制成功；
- 把PPFT或PIDSR结构整体移植过来；
- 继续重复查看同一开发测试集。

## 9. 如果继续双向纠偏

只有state-only oracle通过后，推荐研究：

1. 使用E1第一阶段结果`T1`作为RGB指导，而不是原始带反射RGB。
2. 以实测`Pobs`为锚，预测有界的显式`Δq/Δu`，不从RGB凭空生成偏振。
3. 将偏振重建decoder与下游P2R任务适配器解耦。
4. 每个阶段分别监督，并用同参数量对照隔离信息价值与容量价值。

这些是待验证设计原则，不是文献已经证明的项目结论。

## 10. 与用户协作时的输出要求

- 用户后续任务实行任务点管理。
- 每个正式任务需包含六部分：背景与目标、执行方案、所需资源、风险评估、
  验收标准、执行时间规划。
- 任务文档要能由服务器上的另一个AI独立执行。
- 路径以用户提供的实际V100目录为准，不要自行猜测。
- 优先必要实验和小规模实验；只有硬门通过才扩大规模。

## 11. 阅读顺序

读完本文件后，按顺序阅读：

1. `README.md`
2. `docs/PROJECT_STATUS.md`
3. `docs/METHOD_SNAPSHOT.md`
4. `docs/EVIDENCE_LIMITS.md`
5. `docs/REFERENCES.md`

完成后再与用户讨论方案。不要根据文件名推断旧方案仍有效，以本文件的
当前状态和停止条件为准。

